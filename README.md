Covide Color Conversion API

A .NET Web API that converts HEX color values into multiple color representations including RGB, HSL, HSV, CMYK, and XYZ, with optional color name lookup from a SQLite database.
